import 'package:flutter/material.dart';

class TextWrap extends StatelessWidget {
  const TextWrap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
